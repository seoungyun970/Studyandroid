<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="exampleViewModel"
            type="com.example.androidstudy.chapter38.viewmodel.ExampleViewModel"/>
    </data>

<androidx.constraintlayout.widget.ConstraintLayout

    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".chapter34.LoginRetrofitActivity">

    <EditText
        android:id="@+id/tv_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="bhchoi_giosis"
        android:text="@={exampleViewModel.etId}"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="20dp"
        android:gravity="center"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="12345"
        android:text="@={exampleViewModel.etPw}"
        android:id="@+id/tv_pw"
        app:layout_constraintTop_toBottomOf="@id/tv_id"
        android:textSize="20dp"
        android:gravity="center"/>

    <TextView
        android:id="@+id/user_data"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:layout_marginTop="200dp"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btn_login"
        android:textSize="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="login"/>
</androidx.constraintlayout.widget.ConstraintLayout>

</layout>